{
  "name": "joi-extension-date-within",
  "version": "0.0.3",
  "description": "Adds within validation to enable you to specify how many days a from a value should be",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/*_test.js",
    "coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:jamiemcconnell/joi-extension-date-within.git"
  },
  "keywords": [
    "joi",
    "validation",
    "extension",
    "date",
    "within"
  ],
  "author": "Jamie McConnell <jamie_mcconnell@icloud.com>",
  "license": "MIT",
  "dependencies": {
    "hoek": "^4.0.2",
    "joi": "^9.0.4"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.11.12",
    "istanbul": "^0.4.5",
    "mocha": "^3.0.2"
  }
}
